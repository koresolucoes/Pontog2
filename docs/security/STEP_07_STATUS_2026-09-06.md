# Step 07 — Cleanup & Regression Status — 2026-09-06

## Base

- branch: `security/step-07-cleanup-regression`
- synchronized from production-green `main` at `b034174f8d59f9abb4d7489512f7826a30dcf340`
- rule: authorization/grants/policies and indexes are kept in separate rollbackable migrations.

## Applied migrations

### 07A — Community authorization cleanup

Forward: `21_community_authz_cleanup_forward.sql`
Rollback: `21_community_authz_cleanup_rollback.sql`

Fixed:
- authenticated `USING true` visibility of private communities;
- self-join bypass for private communities;
- member self-promotion through self-update of `community_members.role`;
- tautological cross-community manager predicate;
- duplicated community policies.

Regression evidence:
- 11 communities, 0 private, 15 memberships, 0 pending join requests at cutover;
- synthetic private community visible to anon: 0 rows;
- synthetic private community visible to unrelated authenticated user: 0 rows;
- normal member self-promotion attempt: 0 rows mutated;
- all tests used transaction + rollback.

### 07A.2 — Exact duplicate policy cleanup

Forward: `22_duplicate_policy_cleanup_forward.sql`
Rollback: `22_duplicate_policy_cleanup_rollback.sql`

Removed only policies with an equivalent policy remaining active in conversations, venue reviews/replies, video likes/ratings and videos.

### 07A.3 — Orphan SECURITY DEFINER execution

Forward: `23_function_execute_cleanup_forward.sql`
Rollback: `23_function_execute_cleanup_rollback.sql`

- `update_video_comments_count()` has no attached trigger and no repository consumer.
- authenticated direct `EXECUTE` revoked; physical removal deferred.

### 07B — Critical FK indexes

Forward: `24_fk_indexes_forward.sql`
Rollback: `24_fk_indexes_rollback.sql`

Added targeted indexes for payment/B2B, private albums, claims/check-ins, audit logs and communities. Largest table in this rollout set was ~80 kB before creation.

Important: do not remove these based on immediate `unused_index` advisor output; they have no runtime history yet.

### 07B.2 — Proven duplicate constraints

Forward: `25_duplicate_constraints_forward.sql`
Rollback: `25_duplicate_constraints_rollback.sql`

Removed:
- redundant UNIQUE on `notification_preferences(user_id, notification_type)` while PK remains;
- redundant `unique_user_video_rating` while `video_ratings_video_id_user_id_key` remains.

Explicitly NOT removed: the two `profiles.last_seen` indexes, because their NULL ordering differs.

### 07C — Messaging RLS initplan cleanup

Forward: `26_messaging_rls_initplan_forward.sql`
Rollback: `26_messaging_rls_initplan_rollback.sql`

- narrowed legacy `TO public` policies to `authenticated`;
- converted row-wise `auth.uid()` to `(select auth.uid())`;
- preserved existing membership/sender authorization.

Regression evidence for a real participant:
- baseline expected: 3 conversations / 146 messages;
- authenticated RLS visible: 3 conversations / 146 messages / 3 participation rows;
- anon has no SELECT grant on messaging tables.

Post-change Performance Advisor no longer reports initplan warnings for messages, conversations or conversation_participants.

### 07D — Profile taxonomy / plan policy consolidation

Forward: `27_profile_taxonomy_policy_cleanup_forward.sql`
Rollback: `27_profile_taxonomy_policy_cleanup_rollback.sql`

Preserved product visibility:
- active plans remain public;
- `profile_tribes` remains public profile data;
- `profile_looking_for` remains readable by authenticated users.

Mutations are now explicit owner-only INSERT/UPDATE/DELETE policies using `(select auth.uid())`.

Post-change Performance Advisor no longer reports the previous `plans`, `profile_tribes` or `profile_looking_for` initplan/multiple-policy warnings.

## Residual Step 07 work

1. `increment_b2b_campaign_metric(uuid,text)` can be called repeatedly by any authenticated user and needs event dedupe / antifraud before execute surface can be reduced.
2. `increment_video_views(integer)` has similar lower-risk analytics inflation exposure.
3. Agora, video and review policies still contain `auth_rls_initplan` and overlapping permissive policies; migrate in bounded domain batches.
4. Remaining unindexed FK findings are INFO-level and should be prioritized by real query paths, not added blindly.
5. Extension-owned PostGIS SECURITY DEFINER warnings require dedicated validation; do not mutate extension functions as generic cleanup.
6. New indexes must accumulate runtime history before any unused-index decisions.

## Advisor references

- Auth RLS initplan: https://supabase.com/docs/guides/database/database-linter?lint=0003_auth_rls_initplan
- Unindexed foreign keys: https://supabase.com/docs/guides/database/database-linter?lint=0001_unindexed_foreign_keys
- Multiple permissive policies: https://supabase.com/docs/guides/database/database-linter?lint=0006_multiple_permissive_policies
- Unused indexes: https://supabase.com/docs/guides/database/database-linter?lint=0005_unused_index
